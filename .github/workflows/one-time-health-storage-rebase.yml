name: One-time Health Storage integration migration

on:
  push:
    branches:
      - sentinel-2.9-health-storage-task-integration
    paths:
      - .github/workflows/one-time-health-storage-rebase.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: sentinel-2.9-health-storage-task-integration
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.x'
          cache: false
      - name: Apply retained Mac Health and Storage Graph migration to current mainline
        run: |
          git fetch origin sentinel-2.8-mac-health-storage-graph
          git show origin/sentinel-2.8-mac-health-storage-graph:tools/one_time_mac_health_storage_graph.py > /tmp/health_storage_migrate.py
          python3 /tmp/health_storage_migrate.py
          python3 - <<'PY'
          from pathlib import Path
          p=Path('mac_health_storage.go')
          s=p.read_text().replace('\n    "fmt"\n','\n')
          p.write_text(s)
          PY
          gofmt -w mac_health_storage.go mac_health_storage_contract_test.go
      - name: Validate integrated source
        run: |
          git diff --check
          node --check web/app/lenses/system.js
          node --check web/app/runtime.js
          node --check web/app/task-center.js
          go test ./... -run 'MacHealthAndStorageGraph|Storage|SystemProfile|WorkGate|TaskCenter'
          go vet ./...
          grep -Fq 'Sentinel 2.8 Mac Health + Lazy Storage Graph' mac_health_storage.go
          grep -Fq 'Generate Storage Graph' web/app/lenses/system.js
          grep -Fq '/api/health/live' main.go
          grep -Fq '/api/storage/graph' main.go
          grep -Fq 'Sentinel 2.9 Floating Task Center' web/app/task-center.js
      - name: Full repository test and desktop package
        run: |
          go test ./...
          chmod +x build-desktop-macos.sh
          ./build-desktop-macos.sh
          test -d dist/Sentinel.app
          for engine in dist/Sentinel.app/Contents/Resources/bin/sentinel-macos-arm64 dist/Sentinel.app/Contents/Resources/bin/sentinel-macos-x86_64; do
            LC_ALL=C grep -aFq 'Sentinel 2.8 Mac Health + Lazy Storage Graph' "$engine"
            LC_ALL=C grep -aFq '/api/health/live' "$engine"
            LC_ALL=C grep -aFq '/api/storage/graph' "$engine"
            LC_ALL=C grep -aFq 'Generate Storage Graph' "$engine"
            LC_ALL=C grep -aFq 'Sentinel 2.9 Floating Task Center' "$engine"
          done
      - name: Commit integrated product changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add main.go mac_health_storage.go mac_health_storage_contract_test.go web/app/lenses/system.js web/app/runtime.js
          git commit -m 'Integrate Mac Health and lazy Storage Graph on Task Center mainline'
          git push origin HEAD:sentinel-2.9-health-storage-task-integration
