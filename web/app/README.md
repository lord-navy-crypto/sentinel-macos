# Sentinel 2.4 application frontend

This directory owns the default Sentinel product runtime served at `/`.

- `shell.css` — main application visual system.
- `controller.js` — current controller while lens code is being split into domain modules.

Standalone deep workspaces outside this directory are auxiliary surfaces and must not be required for the default startup path.
